# =============================================================================
# Squadron Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env or .env.local to git

# -----------------------------------------------------------------------------
# Database (Local Development)
# -----------------------------------------------------------------------------
# Local PostgreSQL via Docker (docker compose up -d)
DATABASE_URL="postgresql://squadron:squadron_secret@localhost:5433/squadron?schema=public"

# -----------------------------------------------------------------------------
# Database (Production)
# -----------------------------------------------------------------------------
# Production database is managed automatically by Vercel + Prisma Postgres
# DO NOT set DATABASE_URL in production - Vercel injects it automatically
# See: https://vercel.com/docs/storage/vercel-postgres

# -----------------------------------------------------------------------------
# Privy Authentication (https://console.privy.io)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id
PRIVY_APP_SECRET=your_privy_app_secret

# IMPORTANT: Configure allowed origins in Privy Dashboard:
# - Local dev: http://localhost:3001
# - Production: https://your-domain.vercel.app

# -----------------------------------------------------------------------------
# Ethos Network (https://ethos.network)
# -----------------------------------------------------------------------------
ETHOS_CLIENT_ID=squadron@1.0.0

# -----------------------------------------------------------------------------
# Gemini AI (https://makersuite.google.com/app/apikey)
# -----------------------------------------------------------------------------
# Used for generating squad avatar images
# GEMINI_API_KEY=your_gemini_api_key

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# GITHUB_PAT=your_github_pat_here
